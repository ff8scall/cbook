# 체크리스트: 리디북스 50% 할인 이벤트 추가 수집

## 📅 작성일: 2026-05-01
## 🎯 목표: 리디북스 특정 이벤트 페이지(50% 할인 등)의 데이터를 수집하여 대시보드에 통합

---

### 1단계: 스크레이퍼 로직 설계 및 구현
- [x] `src/scraper/ridi.js` 내 `scrapeRidiEvent(url)` 신규 함수 추가
- [x] 이벤트 페이지 특유의 "참여작" 탭 클릭 및 데이터 로딩 대기 로직 구현
- [x] 무한 스크롤(Infinite Scroll) 대응을 위한 자동 스크롤 및 아이템 카운팅 로직 적용

### 2단계: 데이터 추출 및 정규화
- [x] 신규 셀렉터 적용: 제목(`a.e7z8ge71`), 이미지, 가격 정보 추출
- [x] **정가 역산(Reverse Calculation)**: `할인가 / (1 - 할인율/100)` 공식을 통한 정가 데이터 생성
- [x] "소장" 가격 필터링: "대여" 가격만 있는 경우 제외 또는 소장 가격만 추출
- [x] 수집된 데이터를 마스터 DB 매칭용 형식으로 정규화

### 3단계: 시스템 통합 및 검증
- [x] 기존 `scrapeRidi`와 신규 함수 통합 (여러 URL 동시 수집 지원)
- [x] `npm run scrape:ridi` 실행을 통한 `ridi_sets.json` 갱신 테스트
- [x] 대시보드 UI에서 50% 할인 딜(예: DAYS, 하네배드! 등) 정상 노출 확인
- [x] 중복 데이터 처리 로직 점검 (세트 할인 페이지와 중복될 경우 최저가 우선)

---
**상태**: ✅ 작업 완료 (2026-05-01)
**작업 파일**: `src/scraper/ridi.js`, `public/data/ridi_sets.json`
